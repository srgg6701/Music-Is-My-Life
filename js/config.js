const config = {
    handlers:{}, // will be filled in the loop bellow
    classes: {
        active: 'active',
        hidden: 'hidden'
    },
    values: {
        notChosenYet: ["is not chosen yet.","не выбран."]
    }
};
